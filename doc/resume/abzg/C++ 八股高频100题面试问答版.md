下面给你一份**C++ 八股高频 100 题面试问答版（精简但覆盖核心）**，按模块整理，适合直接背 + 面试前速刷。

------

# 一、C++基础 & 对象模型（1-15）

**1. C++和C的区别？**
 C是面向过程，C++支持面向对象（封装/继承/多态），并引入STL、模板等。

**2. struct和class区别？**
 默认权限不同：struct public，class private。

**3. C++对象内存布局？**
 成员变量 + 对齐 + vptr（若有虚函数）。

**4. 空类大小是多少？为什么？**
 1字节，用于区分对象地址。

**5. this指针是什么？**
 指向当前对象的常量指针。

**6. static成员变量特点？**
 类共享，存储在全局区。

**7. static成员函数特点？**
 没有this指针，只能访问static成员。

**8. extern作用？**
 声明外部变量/函数。

**9. inline函数作用？**
 减少函数调用开销，建议编译器内联展开。

**10. 宏和inline区别？**
 宏是文本替换，inline有类型检查。

**11. 引用和指针区别？**
 引用必须初始化且不可为空，指针可变。

**12. const作用？**
 修饰变量不可修改，修饰函数保证不改成员。

**13. volatile作用？**
 防止编译器优化，常用于硬件/多线程。

volatile 的作用是告诉编译器该变量可能被外部因素改变，从而禁止对其进行优化，每次都必须从内存读取，但它**不提供线程安全和原子性保障**。

**14. mutable作用？**
 允许const对象修改该成员。

**15. 对齐规则？**
 按最大成员对齐。

------

# 二、面向对象（16-30）

**16. 三大特性？**
 封装、继承、多态。

**17. 多态是什么？**
 同一接口不同实现。

**18. 虚函数实现机制？**
 虚函数表vtable + vptr。

**19. 构造函数可以是虚函数吗？**
 不可以。

**20. 析构函数为什么必须是虚函数？**
 防止基类指针删除子类对象出错。

**21. 纯虚函数作用？**
 定义接口，类变抽象类。

**22. 抽象类特点？**
 不能实例化。

**23. override关键字作用？**
 防止函数签名错误。

override 是 C++11 用来标记“重写基类虚函数”的关键字，它可以让编译器检查函数签名是否正确，避免因为参数或签名错误导致的“伪重写”问题，是提升代码安全性和可维护性的最佳实践。

**24. final作用？**
 禁止继承/重写。

**25. 重载 vs 重写？**
 重载：同名不同参数；重写：子类覆盖父类。

**26. 多继承问题？**
 菱形继承、二义性。

**27. 虚继承作用？**
 解决菱形继承数据冗余。

**28. 对象切片是什么？**
 子类赋值给父类时丢失子类信息。

**29. RTTI是什么？**
 运行时类型识别。

**30. dynamic_cast用途？**
 安全向下转型。

在 C++ 里 `static_cast` 和 `dynamic_cast` 都是**类型转换运算符**，但它们的安全性、运行机制和适用场景差别很大

static_cast（静态转换）编译期转换，用于“你确定类型正确”的情况

dynamic_cast（动态转换）运行时类型检查（RTTI）依赖虚函数，更安全，但有RTTI 有性能开销，需要 **vtable（虚函数表）** 来识别运行时真实类型。

------

# 三、内存管理（31-45）

**31. new/delete做了什么？**
 new分配内存+构造对象。

`new / delete` 是 C++ 里用于**动态内存分配与释放**的一对操作符，本质上它们不只是“分配内存”，而是做了一整套对象生命周期管理。

new = 分配内存 + 调用构造函数

delete = 调用析构函数 + 释放内存

new 在堆上分配内存并调用构造函数，delete 调用析构函数并释放内存；底层分别对应 operator new 和 operator delete。



**32. malloc/free和new/delete区别？**
 是否调用构造析构。

malloc/free 只是“内存管理”，申请/释放原始内存，不会调用构造函数

new/delete 是“对象级别操作”， 本质：new = malloc + 构造函数调用，delete = 析构函数 + free

为什么 C++ 还要 malloc？C 兼容，底层系统调用 / 内存池 / allocator 实现

一句话总结

> malloc/free 只负责“内存”，不关心对象；
>  new/delete 负责“内存 + 对象生命周期（构造/析构）”，是 C++ 面向对象语义的一部分



**33. operator new和new区别？**
 operator new只分配内存。

`operator new` 本质是一个**内存分配函数**

`new` 是 **C++ 关键字 / 表达式**，它做的是一整套流程：分配内存 + 构造对象



**34. delete this安全吗？**
 必须确保对象是new创建。

**35. 内存泄漏是什么？**
 分配未释放。

**内存泄漏（Memory Leak）\**指的是：程序在运行过程中\**动态分配了内存，但在不再使用后没有正确释放**，导致这部分内存无法被再次使用，并且随着程序运行持续累积。

如何检测内存泄漏？Visual Studio Diagnostics Tools

在 Visual Studio Diagnostics Tools 中，通过多次拍摄 Memory Snapshot，并进行对比分析，观察对象数量和大小是否持续增长，并结合 Retention Graph 查看引用链，可以定位内存泄漏的对象及其未释放原因。



**36. 野指针是什么？**
 指向非法内存。

指向“未知或无效内存地址”的指针。换句话说，就是这个指针“还在用”，但它指向的内存已经**不再属于合法、可控范围**了。

野指针是怎么产生的？1）未初始化的指针，2）释放后继续使用，3）指向局部变量的地址

如何避免野指针？1）初始化指针，delete 后置空，3）避免返回局部变量地址，4）使用智能指针



**37. 智能指针有哪些？**
 unique_ptr/shared_ptr/weak_ptr。

**38. shared_ptr原理？**
 引用计数控制块。

本质：**引用计数型智能指针（Reference Counting Smart Pointer）**，用于实现“多个指针共享同一块堆内存，并在最后一个引用释放时自动析构对象”。

一句话： shared_ptr = 指针 + 控制块（引用计数 + 管理信息）。它通常由两部分组成：1）指向对象的裸指针（T*）2）控制块（Control Block）

控制块结构

标准库实现类似：

```
struct ControlBlock {
    std::atomic<long> use_count;
    std::atomic<long> weak_count;
    void* ptr;
    Deleter deleter;
};
```

关键点：

> **use_count 是原子变量（线程安全）**

shared_ptr 的关键特性：1.线程安全（注意是“计数安全”，不是对象安全），2.拷贝是“浅拷贝”，3.删除时机：最后一个引用释放 use_count == 0 -> delete object

shared_ptr 的核心问题？ 1.循环引用，解决方案：weak_ptr，2.误用 this

shared_ptr 的代价？1.性能成本：原子操作（引用计数 ++ / --）2.内存成本：控制块额外占用



**39. weak_ptr作用？**
 解决循环引用。

weak_ptr 用来“观察 shared_ptr 管理的对象”，但不增加引用计数，从而打破循环引用并避免内存泄漏。

weak_ptr 的核心能力：1）观察对象是否还存在，2）安全访问对象（lock），3）检测生命周期

weak_ptr 的内部原理：只持有 control block 的 weak_count，不增加 use_count

weak_ptr 使用场景：1）打破循环引用，2）缓存设计，3）观察者模式（Observer），4）线程安全资源访问



**40. 循环引用问题？**
 shared_ptr互相引用导致无法释放。



**41. unique_ptr特点？**
 独占所有权。

`std::unique_ptr` 是 C++11 引入的**独占式智能指针**，核心目标是：**用最小开销实现 RAII 自动资源管理，并保证“唯一所有权”**。

核心特点：独占所有权，同一时刻只能有一个 `unique_ptr` 拥有某个对象，不能拷贝，只能**移动（move）**；轻量级（几乎无额外开销）；自动释放资源（RAII）

`unique_ptr` 本质是：“一个不可拷贝、可移动的 RAII 包装器 + 一个原始指针 + 可选删除器”

unique_ptr 是 C++ 的独占式智能指针，只允许单一所有权，禁止拷贝支持移动语义，利用 RAII 在作用域结束时自动释放资源，零引用计数开销，适用于明确单一生命周期的资源管理场景。



**42. RAII是什么？**
 资源绑定对象生命周期。

把资源的生命周期绑定到对象的生命周期上：对象创建时获取资源，对象销毁时自动释放资源。

RAII 的做法

把资源封装到对象里：

```
class MyInt {
public:
    MyInt() {
        p = new int(10);  // 获取资源
    }

    ~MyInt() {
        delete p;         // 释放资源
    }

private:
    int* p;
};
```

使用时：

```
void func() {
    MyInt obj;  // 进入作用域自动获取资源
}              // 离开作用域自动释放资源
```

👉 关键点：

- **构造函数 = 获取资源**
- **析构函数 = 释放资源**

现代 C++ 的 RAII（重点）：RAII 最典型的实现就是智能指针

std::unique_ptr

std::lock_guard

RAII 的核心价值（面试重点）：1.自动释放资源，2.异常安全，3.代码更清晰，4.避免资源泄漏

RAII 是 C++ 的资源管理思想，即资源的生命周期绑定对象生命周期。通过在构造函数中获取资源，在析构函数中释放资源，实现自动化资源管理，从而避免内存泄漏和异常安全问题。智能指针和 lock_guard 都是 RAII 的典型实现。



**43. 栈 vs 堆？**
 栈自动管理，堆需手动释放。

栈是一种由**编译器自动管理**的内存区域，遵循 **后进先出（LIFO）**。

特点：自动分配 / 自动释放；由编译器管理（函数调用时创建，返回时销毁）；速度非常快（只移动栈指针）；空间相对较小（一般 MB 级）；生命周期严格跟随作用域



堆是由**程序员手动管理**或智能指针管理的内存区域。

特点：手动分配 / 手动释放；使用 `new/delete` 或 `malloc/free`；生命周期由你控制；空间大（可用系统内存）；分配/释放较慢（涉及系统调用 + 内存管理算法）；容易产生内存泄漏



面试一句话：栈是编译器自动管理的快速局部内存，生命周期随作用域结束；堆是由程序员控制的动态内存，用于灵活分配，但需要手动管理，容易产生泄漏和碎片。



**44. 内存对齐原因？**
 提高CPU访问效率。

现代 CPU 读取内存不是按“字节”读，而是按 **字长（word）或缓存行（cache line）**读取，比如：

- 32位 CPU：常见按 4 字节
- 64位 CPU：常见按 8 字节或 16 字节
- Cache line：通常 64 字节

编译器为什么要“强制对齐”？提高访问效率；符合 CPU ABI 约定

总结一句话：为了让 CPU 能以最少的内存访问次数，最高效地按机器字长访问数据，同时避免跨 cache line 或跨总线访问带来的性能损耗甚至硬件异常。



**45. placement new是什么？**
 在指定内存构造对象。

------

# 四、STL（46-60）

**46. vector底层？**
 动态数组。

连续内存的动态数组（dynamic array），由 allocator 管理的一块可扩容的堆内存。

1. 内存结构（核心）

`vector` 内部通常维护三个指针（或等价结构）：

- `start`：指向已分配内存的起始
- `finish`：指向当前最后一个元素之后
- `end_of_storage`：指向整块容量的末尾

可以理解为：

```
[start ........ finish ........ end_of_storage]
 |---- size ----|------ capacity ------|
```

含义：

- `size = finish - start`（已有元素数量）
- `capacity = end_of_storage - start`（总容量）

2. 为什么说是“连续数组”

`vector` 的元素在内存中是**连续存储**的：

```
std::vector<int> v = {1,2,3};
```

内存类似：

```
[1][2][3][ ][ ][ ]
```

👉 这也是它支持：

- O(1) 随机访问 `v[i]`
- CPU cache 友好（性能好）

3. 扩容机制（重点面试点）

当 `size == capacity` 时，再 `push_back` 会触发：

### 步骤：

1. 申请一块更大的新内存（通常 **2倍扩容** 或 1.5倍）
2. 将旧数据 **拷贝/移动** 到新内存
3. 释放旧内存
4. 更新指针

```
旧： [1 2 3]
新： [1 2 3 _ _ _ _ _]
```

4. 扩容代价（为什么 push_back 有时慢）

单次 `push_back`：

- 平均 O(1)
- 但扩容时是 O(n)

👉 所以是 **均摊 O(1)**（amortized O(1)）

5. allocator（内存管理）

`vector` 不直接 new/delete，而是通过：

```
std::allocator<T>
```

作用：

- 分配原始内存（allocate）
- 构造对象（construct）
- 销毁对象（destroy）

👉 分离“内存”和“对象生命周期”



6. iterator 失效问题（高频面试）

由于扩容会整体搬家：



面试一句话总结：

vector 底层是基于连续内存的动态数组，通过 capacity 管理扩容，当空间不足时重新申请更大内存并搬移数据，因此支持高效随机访问，但在扩容和中间插入删除时存在性能开销，同时会导致迭代器失效。



### 会失效：

- 所有 `iterator`
- 所有 `pointer`
- 所有 `reference`

```
auto it = v.begin();
v.push_back(100); // 可能触发扩容
// it 失效
```

`allocator`（分配器）本质上是：

> **C++ 用来“管理内存分配与对象构造/销毁”的一层抽象接口。**

它是 STL 容器（`vector / list / map`）背后真正做内存工作的“底层工具”。



**47. vector扩容机制？**
 通常2倍扩容。



**48. vector push_back复杂度？**
 均摊O(1)。



**49. deque底层？**
 分段连续数组。

deque = 分段连续内存 + 中央控制表（map）

它不是一整块连续内存，而是：

- 多个“小连续数组”（buffer / block）
- 一个“指针数组”（map）管理这些块

为什么要这样设计？

1. 解决 vector 的问题

vector：

- ❌ 扩容要整体搬家（realloc）
- ❌ 头部插入 O(n)

deque：

- ✔ 头尾插入 O(1)
- ✔ 不需要整体搬迁

2. 为什么不用链表？

链表：

- ❌ cache 不友好（指针跳转）
- ❌ 内存碎片严重

deque：

- ✔ 每块是连续内存（cache 友好）
- ✔ 又能动态扩展

总结一句面试答案: deque 底层是由多个固定大小的连续内存块组成，并通过一个指针数组（map）进行管理，从而实现了头尾插入 O(1) 和随机访问 O(1)，但整体并不是连续内存。



**50. list底层？**
 双向链表。



**51. map底层？**
 红黑树。

**52. unordered_map底层？**
 哈希表。

**53. map vs unordered_map？**
 logn vs O(1)。



**54. hash冲突解决？**

哈希冲突（Hash Collision）指的是：**不同的 key 经过 hash 函数计算后，落到了同一个桶（bucket）中**。

哈希冲突的常见解决方案:1️⃣ 链地址法（Separate Chaining）⭐最常用；2️⃣ 开放定址法（Open Addressing）；3️⃣ 再哈希（Rehashing / 扩容）；4️⃣ 红黑树替代（Java 8 思路）



**55. STL迭代器是什么？**
 泛化指针。

STL 迭代器（Iterator）可以理解为**“统一访问容器元素的指针抽象”**，它是 C++ STL 设计中非常核心的一层封装。

你可以把它先记住一句话：

> **迭代器 = 用来遍历 STL 容器的“泛化指针”**



**56. vector删除元素问题？**
 迭代器失效。



**57. emplace_back作用？**
 原地构造。

**58. sort底层？**
 introsort。



**59. stable_sort作用？**
 稳定排序。

**60. priority_queue底层？**
 堆。

------

# 五、C++11/14/17（61-75）

**61. auto是什么？**
 类型推导。

`auto` 是 C++11 引入的**类型自动推导关键字**，用于让编译器根据初始化表达式**自动推断变量类型**。



**62. decltype作用？**
 获取表达式类型。

`decltype` 是 C++11 引入的一个**类型推导关键字**，作用是：**在编译期获取表达式的类型**。

它和 `auto` 类似，但本质区别是：

- `auto`：通过初始化值“推导变量类型”
- `decltype`：直接“推导表达式的类型”（不一定要初始化）



**63. lambda是什么？**
 匿名函数对象。

在 C++ 里，**lambda（匿名函数）** 是一种“可以在代码中就地定义的临时函数对象”，本质上是编译器帮你生成的一个**匿名仿函数（functor）类**。

捕获外部变量：值捕获 `[=]`；引用捕获 `[&]`；指定捕获；

mutable 关键字：默认值捕获是只读的：加 mutable 才能改副本：

lambda 本质

编译器会把它转成类似这样：

```
class __Lambda {
public:
    int operator()(int a, int b) const {
        return a + b;
    }
};
```

👉 所以 lambda 本质是：

> **一个重载了 operator() 的匿名类对象**



面试高频总结

1️⃣ lambda 是什么？

👉 匿名函数对象（函数 + 状态）

2️⃣ 和函数指针区别？

- lambda 可以捕获变量
- 函数指针不能

3️⃣ capture 有哪几种？

- `[=]` 值捕获
- `[&]` 引用捕获
- `[a, &b]` 指定捕获

4️⃣ 本质是什么？

👉 编译器生成的 functor 类

一句话总结

👉 **lambda 是 C++ 中“可以携带上下文的匿名函数对象”，本质是语法糖 + 函数对象封装。**



**64. 捕获方式？**
 值捕获/引用捕获。



**65. move语义？**
 资源转移。

**Move 语义（移动语义，Move Semantics）\**是 C++11 引入的一个核心特性，用来\**避免不必要的深拷贝，提高性能**，尤其是在涉及临时对象、大对象（vector、string、unique_ptr）时非常关键。

Move 语义的核心：

> **不是复制资源，而是“窃取（steal）资源”**



**66. std::move做了什么？**
 强制转换为右值。

👉 `std::move` 的本质是：**把一个左值强制转换为右值引用（rvalue reference）**

它到底“做了什么”

✔️ 1. 类型转换（唯一真正做的事）

把：

- 左值（lvalue）
- → 转换为右值引用（rvalue reference）

例如：

```
std::string a = "hello";
std::string b = std::move(a);
```

这里：

- `a` 仍然存在（没有被搬走）
- 只是 `std::move(a)` 让 `a` **变成“可被移动的右值”**

❗重点：std::move 不会移动数据

很多人误解：

> ❌ std::move = 移动数据
>  ❌ std::move = 拷贝到新地方

正确理解：

> ✅ std::move = “允许资源被偷走的权限标记”

三、真正“移动”的发生在哪里？

真正的资源转移发生在：

👉 **移动构造函数 / 移动赋值运算符**

例如：

```
std::string b = std::move(a);
```

实际调用的是：

```
string(string&& other); // move constructor
```

在这个函数里才会：

- 指针直接接管
- 原对象置空



常见面试总结答案（可以直接背）

> `std::move` 本质是一个类型转换工具，它把左值转换为右值引用，从而让编译器可以匹配移动构造函数或移动赋值函数。它本身不移动任何资源，真正的资源转移发生在移动构造/移动赋值内部。



**67. 右值引用是什么？**
 绑定临时对象。

右值引用（rvalue reference）是 C++11 引入的一个核心特性，用来**绑定“临时对象（右值）”并支持移动语义**，从而避免不必要的拷贝，提高性能。

经典面试一句话总结

👉 **右值引用就是用来绑定临时对象，并通过移动语义避免深拷贝的一种引用类型（&&）**



**68. 完美转发？**
 std::forward保持参数属性。

**完美转发（Perfect Forwarding）\**是 C++11 引入的一个机制，用来在函数模板中\**“原封不动地”传递参数的值类别（左值/右值）**，避免多余拷贝，同时保持语义正确。

总结一句话（面试必背）

👉 **完美转发 = 利用模板 + 引用折叠 + std::forward，在函数转发参数时保持原始左值/右值属性不变**

std::move 是“无条件转右值”，std::forward 是“有条件转发值类别”



**69. nullptr是什么？**
 空指针类型安全替代。



**70. constexpr作用？**
 编译期计算。



**71. auto_ptr为什么废弃？**
 拷贝语义错误。



**72. range-based for？**
 for(auto x : container)



**73. emplace优势？**
 避免临时对象。



**74. std::function是什么？**
 函数包装器。

`std::function` 是 C++11 提供的一个**通用可调用对象包装器（callable wrapper）**，用来“统一存储和调用各种可调用实体”。

👉 **`std::function` 就是“函数的万能盒子”**

它可以装：

- 普通函数
- Lambda 表达式
- 函数对象（仿函数）
- `std::bind` 结果
- 成员函数绑定后的可调用对象

`std::function` 是基于类型擦除实现的通用可调用包装器，它通过统一调用接口屏蔽不同 callable 类型的差异，从而实现运行时多态，但代价是额外的间接调用与潜在的内存开销。



**75. bind作用？**
 参数绑定。

👉 本质是：

> 生成一个“函数对象（仿函数）”

## ⭐ std::bind 做了什么？

> 通过参数绑定 + 占位符机制，把一个可调用对象转换成新的可调用对象，从而实现“部分参数固定 + 参数重排”。

------

## ⭐ 面试加分回答版本：

> std::bind 本质是一个函数适配器，它通过类型擦除和仿函数封装，将原始可调用对象与部分参数绑定，生成新的可调用对象。由于 lambda 的出现，std::bind 已逐渐被替代，但在函数适配和旧接口兼容中仍然有价值。



**函数对象（仿函数，Functor）** 本质上就是：

> 👉 **“重载了 `operator()` 的类或结构体对象，使其可以像函数一样被调用。”**



------

# 六、多线程（76-90）

**76. 线程创建方式？**
 std::thread / Qt QThread。

**77. mutex作用？**
 互斥访问共享资源。

**78. lock_guard作用？**
 自动加锁解锁。

**79. unique_lock区别？**
 可延迟加锁。

**80. 死锁是什么？**
 循环等待资源。

**81. 如何避免死锁？**
 加锁顺序一致。

**82. 条件变量作用？**
 线程通信。

**83. atomic是什么？**
 原子操作。

**84. CAS是什么？**
 比较并交换。

**85. volatile能保证线程安全吗？**
 不能。

**86. 线程同步方式？**
 mutex/condition_variable/atomic。

**87. 多线程为什么快？**
 并行执行。

**88. 线程上下文切换成本？**
 寄存器+栈切换开销。

**89. 并发 vs 并行？**
 并发是交替执行，并行是真同时。

**90. lock-free是什么？**
 无锁算法。

------

# 七、编译&系统（91-100）

**91. 编译四阶段？**
 预处理->编译->汇编->链接。

**92. 静态库 vs 动态库？**
 编译进程序 vs 运行加载。

**93. ODR是什么？**
 单一定义规则。

**94. 头文件作用？**
 声明接口。

**95. inline为什么可能不生效？**
 编译器决定。

**96. segmentation fault原因？**
 非法内存访问。

**97. 栈溢出原因？**
 递归过深。

**98. ABI是什么？**
 二进制接口规范。

**99. 编译优化等级？**
 -O0 -O2 -O3。

**100. gdb作用？**
 调试工具。